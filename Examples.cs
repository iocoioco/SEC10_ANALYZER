var text = "this is a \n test \n to check the working process";
CenterHudForm.Show(
    text: text // g.date.ToString("yyyy-MM-dd"),
               //durationMs: 5000,
               //fontSize : 43
);

MouseHudForm.Show(
    text: text // g.date.ToString("yyyy-MM-dd"),
               //durationMs: 3000,
               //fontSize : 31
);


